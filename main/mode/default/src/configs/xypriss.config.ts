import { Router } from "xypriss";

/**
 * XyPriss Default Server Configuration
 * 
 * Configured for a standard, single-instance server operation. 
 * This setup is ideal for simple APIs or monolithic services where 
 * multi-server isolation is not required.
 */
export const serverConfigs: ServerOptions = {
  // Default options are automatically inherited
};
