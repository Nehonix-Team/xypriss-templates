TARGET: src/configs/xypriss.config.ts

@INJECT: // -->{{SECURITY_CONFIG}}
  security: {
    terminalOnly: { 
      enable: true, 
      allowedTools: ["postman", "curl", "insomnia", "axios"] 
    }
  },
@END
